# Backlog

Статус: `Active / CORE-ALIGN execution` — `AMS-REALTBASE-CORE-ALIGN` v1 APPROVED.

Требования и acceptance эпиков находятся в `AMS_MASTER_PLAN №4.md`. Операционный граф — stealth Task Manager (`ral`).

## Фокус

EPIC-01…08: демо-канон local PostgreSQL + MEDIA_DIR (без закупки БД), proof Next 16 `proxy.ts`, канон Public/System Gateway, env fail-fast, cache/18A на существующих verify, UI composition без redesign, drift guards, ISR/cleanup.

## Политика доставки

- Каждый EPIC: новая ветка от `main`, SourceCraft PR, `MERGE_AFTER_GATE`, удаление ветки.
- Production и GitHub не входят в этот план.

## Исторический CORRECTIONS v3

Закрыт на `main@14e9bf53` (EPIC 11–21). Не исполнять заново.

## Исторический HARDENING v2

Закрыт на `main@f39826c` (EPIC 0–10).
