/**
 * Approved parameterized SQL for ingest claims, SKIP LOCKED, and bounded bulk updates.
 * Generic query(sql: string) is not exported.
 */
export const ingestSqlLayer = "src/core/data-access/ingest/sql" as const;
